
    
    <div class="background"></div>
    <div class="overlay"></div>

    <div class="container">
        {{#each sorteado}}
            <h1>Resultado do Sorteio</h1>
            <h3>{{name}}</h3>
            <table>
                <thead>
                    <tr>
                        <th>Itens</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>{{items}}</td>
                    </tr>
                </tbody>
            </table>
        {{/each}}
        <a href="/{{guestId}}" class="btn">Voltar</a>
    </div>

