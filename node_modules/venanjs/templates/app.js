import { Venan } from "venanjs";
const app = new Venan()


// Delete this line of code when you need to write the routes
app.serveStatic('public');


app.listen(3001, () => {
  console.log('Server is running on http://localhost:3001');
});